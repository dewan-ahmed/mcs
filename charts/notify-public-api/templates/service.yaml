apiVersion: v1
kind: Service
metadata:
  name: {{ include "notify-public-api.fullname" . }}
  labels:
    {{- include "notify-public-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notify-public-api.selectorLabels" . | nindent 4 }}
