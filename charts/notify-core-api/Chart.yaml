apiVersion: v2
name: notify-core-api
description: Internal notification command API
type: application
version: 0.1.0
appVersion: "0.1.0"
# dependencies:
#   - name: devops-library
#     version: 0.1.0
#     repository: oci://registry.example.com/charts
# The devops-library chart is resolved from an OCI registry at package time.
